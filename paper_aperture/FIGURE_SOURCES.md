# FIGURE_SOURCES — panel → source table → repo provenance

**Manuscript:** *The information aperture of a bucket record* (R40 frozen architecture,
`docs/ROUND63_GPT_ROUND40_RULING_RAW.md`).
**Working dir:** `D:\GI_another\paper_aperture\` (the superseded R36 skeleton in
`paper_flagship\` is untouched, kept as archive).

**RULE ZERO (R40 writing order step 3):** every plotted number has a machine-readable
source table under `source_tables/`; no prose values are typed by hand into a figure
script. Tables are regenerated by `source_tables/extract_source_tables.py` (reads the
original repo evidence, cwd = repo root). Figures read **only** from `source_tables/`.

Provenance classes:
- **COPIED** — transcribed programmatically from a machine-readable JSON/CSV in this repo.
- **FROZEN** — transcribed from a frozen prose/tex document in this repo (canonical
  machine source is outside GI_a2, in the `E:\GAN_FCC_WORK` program repo); every such
  value cites file + section and is a frozen-verbatim number.
- **DERIVED** — computed here from the tables above (Fig 1 atlas summary).

---

## FIG 1 — master information atlas (the gate)

| element | source table | repo provenance | class |
|---|---|---|---|
| whole atlas geometry (row extents, markers, legend line) | `T_fig1_atlas.json` | DERIVED from the tables below | DERIVED |
| mean formal support = B_p (extent 1.0) | `T_fig1_atlas.json` | first-moment wall, `paper2/main_m1.tex` / covariance model `RISK_GATES_REPORT.md` | DERIVED |
| covariance formal support = B_p⊕B_w (extent 2.0) | `T_fig1_atlas.json` | aperture law, `G3_results.json` partA (18.76×) | DERIVED |
| covariance Fisher-usable rim ≈1.1 | `T_fig1_atlas.json` | `P1_results.json` (5% mode coverage) + `RISK_GATES_REPORT.md` | DERIVED |
| green M1 operating marker | `T_fig1_atlas.json` → markers | `T_fig2_ridge_points.json` + `T_fig2_cohorts.json` | DERIVED |
| red 1.8× killed marker | `T_fig1_atlas.json` → markers | `T_fig3_p1.json` / `T_fig3_map.csv` (claim 1.8 witness f_rec 0.05) | DERIVED |
| amber 1.25× pocket marker (filled/hollow) | `T_fig1_atlas.json` → markers | `T_fig3_pocket.json` (branch PENDING) | DERIVED |

Outputs: `figures/fig1.pdf/png` (pending), `fig1_pocketA.pdf/png` (filled = achieved),
`fig1_pocketB.pdf/png` (hollow = Fisher-feasible, unrealized).

## FIG 2 — mean-layer certificate

| panel | source table | repo provenance | class |
|---|---|---|---|
| (a) information ridge curves | `T_fig2_ridge_curves.csv` | `results/round63_theory/fisher_ridge.csv` | COPIED |
| (a) ridge peaks + operating point | `T_fig2_ridge_points.json` | `results/round63_theory/fisher_ridge.json`; ridge law + jitter cap + operating point from `paper2/main_m1.tex` §3–4/§6 & `supplement_m1.tex` S4 (via `docs/FLAGSHIP_MATERIALS_MAP.md` §1a/§9a) | COPIED + FROZEN |
| (b) two cohort statistics (+1.87 dB / 19.13×, LBs, n/24) | `T_fig2_cohorts.json` | `docs/FLAGSHIP_MATERIALS_MAP.md` §9a (R19-frozen); canonical `M1_VERDICTS_SPEC_CORRECTED_R19.json` in `E:\GAN_FCC_WORK` | FROZEN |
| (c) five-intervention closure strip | `T_fig2_closure.csv` | `docs/SOFTWARE_SATURATION_VERDICT.md` (table) + `docs/FLAGSHIP_MATERIALS_MAP.md` §3a | FROZEN |

Output: `figures/fig2.pdf/png`. No method names/audit codes in the figure
(interventions labelled by neutral mechanism phrase).

## FIG 3 — covariance-layer law and boundary map

| panel | source table | repo provenance | class |
|---|---|---|---|
| (a) spectral law / exact mean wall duel | `T_fig3_aperture.json` → mean_wall_duel_16x16 | `results/round63_next/FOG_DMD_PROBE/FOG_DMD_PROBE_REPORT.md` §14 (E8) | FROZEN |
| (b) 18.76× in/out separation | `T_fig3_aperture.json` → separation_64x64 | `results/round63_next/FOG_DMD_PROBE64/G3_results.json` partA | COPIED |
| (c) 81-cell Fisher atlas + REGION_EMPTY | `T_fig3_map.csv` + `T_fig3_map_meta.json` | `results/round63_next/FOG_DMD_PROBE64/LIVING_REGION_MAP.json` grid[] | COPIED |
| (c) P1 bar (0.70 mode-coverage) | `T_fig3_p1.json` | `results/round63_next/FOG_DMD_PROBE64/P1_results.json` | COPIED |
| (c) pocket highlight | `T_fig3_pocket.json` | `LIVING_REGION_MAP.json` grid + `LIVING_REGION_MAP.md` §4 | COPIED |
| (d) branch inset (A/B) | `T_fig3_pocket.json` → BRANCH | `LIVING_REGION_MAP.md` §4 + R40 ruling §4 | COPIED |

Outputs: `figures/fig3.pdf/png` (pending), `fig3_branchA.pdf/png` (POCKET_ACHIEVED),
`fig3_branchB.pdf/png` (ESTIMATOR_GAP_PERSISTS).

---

## Missing / unrecoverable raw inputs (named precisely)

1. **16×16 aperture pair (in-coverage 0.073 vs out 1.213).** The R40 task listed this for
   `T_fig3_aperture`. **Not recoverable from `D:\GI_another`.** No committed result produces
   this pair; the digits 0.073 and 1.213 occur only coincidentally in unrelated files
   (`paper3_scatter`/`round63_study2` `T_s` ≈ 0.073 and `select_max_s` = 1.213 — a scatter
   experiment, not an aperture test). The canonical aperture-law validation retained is the
   **G3 64×64 18.76× separation** (in 0.250 / out 4.697), plus the **E8 exact-mean-wall duel**
   (fresh band-limited patterns pinned at rel-err 1.000). Recorded in
   `T_fig3_aperture.json → missing_inputs`.

2. **M1 operating-map raw grids** (R14–R19 dead-time/dwell campaign). The GI_a2 repo holds the
   ridge *shape* (`fisher_ridge.csv/json`) and the frozen certified operating-point statistics,
   which is all Fig 2 needs. The full per-scene/per-dwell operating grids and
   `M1_VERDICTS_SPEC_CORRECTED_R19.json` live in the `E:\GAN_FCC_WORK\data_warehouse` program
   repo (pointer only; not required for these figures).
