% =============================================================================
% SUPPLEMENT NOTE S1 -- Unified notation and hidden-state experiment  (R35 s9)
% =============================================================================
% CONTENT MANIFEST (R35 s9 "S1"):
%   * Full score identities (one hidden-state identity; detector + medium
%     specializations -- NOT branded E1/O1, R35 cut 3).
%   * Regularity assumptions.
%   * Complete vs observed experiments.
%   * Dimensions and the notation crosswalk.
%
% PORTED WHOLESALE FROM:
%   * paper_flagship/NOTATION.tex  -> the unified symbol table (gap G1). \input it.
%   * paper2/supplement_m1.tex S7 (proofs, regularity conditions).
%   * paper/main_oe.tex s2-s3 (physical model, RQL) for dimensions.
%   * R33 (materials map s4b) for the joint two-ledger identity statement.
%
% This is a STUB. Section/eq/table numbering carries the S-prefix; wire the
% preamble to match paper2/supplement_m1.tex when assembling the full supplement.
% =============================================================================
\section{Unified notation and hidden-state experiment}
\label{supp:notation}

% \input{../NOTATION.tex}  %% the gap-G1 table body (strip its \documentclass
%                             wrapper first, or \input only the longtable).

% TODO: install one hidden-state score identity with detector + medium
% specializations; regularity assumptions; complete-vs-observed experiments;
% dimensional crosswalk. Credit Louis / Orchard-Woodbury (materials map s8c).
