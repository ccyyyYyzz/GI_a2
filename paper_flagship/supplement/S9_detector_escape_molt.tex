% =============================================================================
% SUPPLEMENT NOTE S9 -- Detector-side escape outside the count-only class  (R35 s9)
% =============================================================================
% FROZEN NOTE TITLE (R35 s4.4):
%   "Detector-side escape beyond the count-only model: a real operator with
%    insufficient matched-resource image strength."
%
% CONTENT MANIFEST (R35 s9 "S9"):
%   * Full MOLT theorem (Masked Laplace-Power-Sum Theorem -- name discipline,
%     register s9c: NOT "elementary-symmetric-polynomial theorem").
%   * Determinant / coherence boundary.
%   * Dose law.
%   * The three image-level kill computations.
%
% R35 s4.4 disposition: MOLT is CUT ENTIRELY from the main narrative; it is too
%   interesting for one parenthetical and too orthogonal for a main section. It
%   is self-contained HERE. The main Discussion carries ONE sentence pointing to
%   this note as evidence that a mathematically new channel is not sufficient when
%   it is Fisher-weak. NO MOLT acronym / dose table / null-pair figure in the body.
%
% PORTED WHOLESALE FROM:
%   * R31 (docs/ROUND63_GPT_ROUND31_RULING_RAW.md, 982 ln) + PRO appendix
%     (..._PRO_APPENDIX_0.md): Masked Laplace-Power-Sum Theorem; saturation ridge
%     t*=2.821439; Jacobian rank doubling 51->102; correlated-speckle determinant
%     law; n_eff^2 photon-dose law; quenched floor.
%   * results/round63_next/SATURATION_JAILBREAK_PROBE/JAILBREAK_PROBE_REPORT.md
%     (R32 six-bar 4 PASS / 2 CONDITIONAL; noiseless witness +268.9 dB;
%     matched-photon fails by orders of magnitude).
%   * results/round63_next/MOLT_REVIVAL_CALC/REVIVAL_CALC.md (STAY DEAD; best
%     honest-dose margin 4.13x vs 10x needed).
% FROZEN authorized/forbidden fence: register s9f (R32).
% =============================================================================
\section{Detector-side escape beyond the count-only model: a real operator with
insufficient matched-resource image strength}
\label{supp:molt}

% TODO: full Masked Laplace-Power-Sum theorem; determinant/coherence boundary;
% dose law; three image-level kill computations. Keep to the R32-authorized
% narrow claim (photon-for-rank trade); forbidden claims per register s9f.
