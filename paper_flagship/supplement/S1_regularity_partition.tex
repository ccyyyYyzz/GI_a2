% =============================================================================
% SUPPLEMENT NOTE S1 -- Regularity conditions and joint missing-information proof
% (R36 s6: "S1 regularity conditions and joint missing-information proof")
% =============================================================================
% R36 CROSSWALK: this note is NEW-scoped; it takes the joint-partition proof that
%   R35 split across old S1 (notation) + old S2 (detector specialization).
%   The unified notation table (gap G1) is now front matter of supplement.tex,
%   not an S-note (R36 s6 has no notation note).
%
% CONTENT MANIFEST (R36 s6 "S1"):
%   * Regularity/support conditions for the separated hierarchy
%     p(y,a|x,theta)=p(y|x,a) p_theta(a).
%   * Proof of the joint observed-information partition
%     I_Y(x,theta) = diag(C_x,C_theta) - E Cov([U_x;U_theta] | Y).
%
% PORTED WHOLESALE FROM:
%   * R33 Thm 1 (materials map s4b): I_Y = diag(C_x,C_theta) - L, L>=0.
%   * paper2/supplement_m1.tex S7 (score/total-variance machinery, regularity).
%   * Louis / Orchard-Woodbury lineage (materials map s8c) credited, not claimed.
% =============================================================================
\section{Regularity conditions and the joint missing-information partition}
\label{supp:partition}

% TODO: regularity/support conditions; proof of Eq.(main 2) partition; L>=0.
% Credit Louis (10.1111/j.2517-6161.1982.tb01203.x); classical identity NOT new.
