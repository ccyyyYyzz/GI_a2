% =============================================================================
% SUPPLEMENT NOTE S3 -- Ridge asymptotics and verification  (R35 s9)
% =============================================================================
% CONTENT MANIFEST (R35 s9 "S3"):
%   * The exact cubic.
%   * Asymptotic matching.
%   * The 1/3 and -2/3 derivations in frozen notation.
%   * Blind runs.
%   * Monte-Carlo convergence.
%   * R16 estimator-artifact closure  (gap G10: full 2M-frame run still open).
%
% PORTED WHOLESALE FROM:
%   * paper2/supplement_m1.tex S4 (jitter_sfi_v2 100k frames; peaks
%     {22.297,10.739,5.173,...}; pooled slope -0.658; score-identity resolves
%     +8.8% histogram bias; k=4/3 refuted).
%   * paper2/main_m1.tex s3 (Thm 3 cubic, small-jitter expansion, crossover).
%   * paper/main_oe.tex s4.2 (cube-root law, peak J, rho* exact table).
%   * results/round63_next/R16_DIAGNOSTIC_RUN/ (R16 diagnostic; gap G10 flag).
% =============================================================================
\section{Ridge asymptotics and verification}
\label{supp:asymptotics}

% TODO: exact cubic + matched asymptotics; (1/3,-2/3) derivations; blind
% verification tables; MC convergence; R16 estimator-artifact closure with the
% carry-forward flag that the full 2M-frame confirmatory run remains open (G10).
