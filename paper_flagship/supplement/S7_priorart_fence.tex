% =============================================================================
% SUPPLEMENT NOTE S7 -- Prior-art fence
% (R36 s6: "S7 ruthless prior-art fence, including DCS/DWS, pilot GI,
%  self-calibration, AO and radio telemetry")
% =============================================================================
% R36 CROSSWALK: NEW dedicated note (R35 folded prior art into related-work prose
%   and the DLGI note). R36 pulls the full fence into its own supplement note so
%   the main-text Scope section (R36 s5) needs only ONE paragraph.
%
% CONTENT MANIFEST (R36 s6 "S7"):
%   * DCS/DWS (dedicated intensity-fluctuation medium sensing).
%   * Pilot/interpolated GI monitoring (reference-arm gain tracking).
%   * Self-calibration / blind bilinear calibration.
%   * AO telemetry + radio self-calibration (calibration-as-science).
%   * The exact residual claim boundary + FORBIDDEN novelty "firsts".
%
% PORTED WHOLESALE FROM:
%   * R33 fence DOIs (materials map s8c): Louis; Cox-Reid; Pine (DWS); Boas-
%     Campbell-Yodh + Durduran (DCS); Bandyopadhyay (SVS); Yang (pilot GI);
%     Xiao/Peng/Zhou-Chen (dynamic-medium GI); Intema/Mevius/Loi (radio selfcal);
%     Laidlaw (AO); Ling-Strohmer + Cambareri-Jacques (blind calibration).
%   * FORBIDDEN "firsts" list: register B3 (R33 s5 + R31 s6).
% =============================================================================
\section{Prior-art fence}
\label{supp:priorart}

% TODO: ruthless fence for DCS/DWS, pilot GI, self-calibration, AO/radio
% telemetry. State the residual claim: simultaneous scene + medium from the same
% programmable GI record with a joint information certificate -- no "firsts"
% beyond that (register B3).
