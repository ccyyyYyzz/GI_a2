% =============================================================================
% SUPPLEMENT NOTE S7 -- Canonical-confusion reciprocity and DLGI estimator (R35 s9)
% =============================================================================
% CONTENT MANIFEST (R35 s9 "S7"):
%   * Proofs (Canonical-Confusion Ledger Reciprocity Theorem).
%   * Pseudoinverses.
%   * Path / hyperparameter distinction.
%   * Likelihood.
%   * State-space implementation (Kalman/RTS -- lives HERE, not in the body,
%     R35 s7.2).
%   * Schedule matrices.
%   * DCS / pilot technical comparison.
%
% THEOREM NAME DISCIPLINE (register s9c / R33 s7): the theorem is the
%   "Canonical-Confusion Ledger Reciprocity Theorem"; the METHOD is DLGI. Keep
%   them distinct. FORBIDDEN claims: no "information conservation", no "free
%   medium information", no additive conservation law (register s9c).
%
% PORTED WHOLESALE FROM:
%   * R33 (docs/ROUND63_GPT_ROUND33_RULING_RAW.md) Thm 1-2 statements
%     (materials map s4b): I_Y = diag(C_x,C_theta) - L; K=A^{-1/2}CB^{-1/2};
%     det(J_x)/det(A)=prod(1-kappa_i^2)=det(J_theta)/det(B).
%   * results/round63_next/DUAL_LEDGER_PROBE/DUAL_LEDGER_PROBE_REPORT.md +
%     dual_ledger_results.json + t3_reciprocity.json (feasibility; frozen).
%   * results/round63_next/LADDER_PROBE/ (ch.2 temporal-prior B=1 smoother
%     +2.2-2.5 dB = the DLGI ch.2 data-term lineage, materials map s5c).
% =============================================================================
\section{Canonical-confusion reciprocity and DLGI estimator}
\label{supp:reciprocity}

% TODO: full reciprocity proof + pseudoinverse version; path-vs-hyperparameter
% distinction; joint likelihood; Kalman/RTS state-space implementation; schedule
% matrices (A,B,K); DCS/pilot technical comparison. Oracle floors:
% sd(t_c)/t_c >= sqrt(2 t_c/T), sd(CV)/CV >= sqrt(t_c/2T) (materials map s4b).
