% =============================================================================
% SUPPLEMENT NOTE S5 -- Auxiliary hidden-state and schedule derivations
% (R36 s6: "S5 auxiliary hidden-state and schedule derivations")
% =============================================================================
% R36 CROSSWALK: this holds miscellaneous derivations that are not the main proof
%   line. R36 s6 gives MOLT no dedicated note, but the executive keeps MOLT
%   complete "in Supplementary Notes S1-S10"; MOLT (R35 old S9) is therefore
%   FOLDED HERE as a clearly-labeled auxiliary subsection.
%
% CONTENT MANIFEST (R36 s6 "S5"):
%   * Auxiliary hidden-state derivations (schedule matrices; ch.1 timestamp
%     refutation aside).
%   * Schedule-ordering derivations behind the paired/random/ordered set.
%   * (Folded) MOLT detector-side escape: Masked Laplace-Power-Sum Theorem and
%     its three image-level kill computations (RETIRED result, kept for record).
%
% PORTED WHOLESALE FROM:
%   * results/round63_next/DUAL_LEDGER_PROBE/ (schedule matrices, ||K|| invariance).
%   * results/round63_next/LADDER_PROBE/ (ch.1 timestamp 11-15% recovery aside).
%   * R31/R31-PRO (MOLT theorem) + SATURATION_JAILBREAK_PROBE + MOLT_REVIVAL_CALC
%     (materials map s5a). MOLT fence: register B6 (R32 authorized/forbidden).
% =============================================================================
\section{Auxiliary hidden-state and schedule derivations}
\label{supp:auxiliary}

% TODO: schedule-matrix derivations; ch.1 timestamp-refutation aside (count pins
% summed holds; 11-15% recovery).

\subsection{Detector-side escape beyond the count-only model (retired)}
% TODO (folded from R35 S9): Masked Laplace-Power-Sum Theorem; determinant/
% coherence boundary; dose law; three image-level kill computations. This channel
% is Fisher-weak at matched photon budget -> not a viable escape. Narrow
% R32-authorized claim only (register B6). No MOLT acronym in the main paper.
