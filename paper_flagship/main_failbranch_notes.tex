% =============================================================================
% FAIL-BRANCH NOTES -- law/boundary manuscript (R36 s7 + R35 s8)
% =============================================================================
% MECHANISM DECISION (documented in BUILD_STATUS.md):
%   Under R35 the two branches shared a spine and differed by a few elements, so
%   inline %% PASS-BRANCH / %%>> FAIL-BRANCH tags in main.tex were adequate.
%   Under R36 the fail branch SWITCHES PROTAGONIST -- different thesis, different
%   section order, different figures, different page cap. Inline tagging would
%   force two structurally different papers into one file and defeat the R36
%   slimming. Therefore the fail branch is a SEPARATE, SELF-CONTAINED SPEC HERE.
%   main.tex is now the clean pass-branch DLGI paper with NO inline fail tags.
%
% This file is NOT compiled and NOT \input by main.tex. It is the build-order
% spec for the fallback paper, to be promoted to its own main_law.tex if any
% DLGI confirmatory bar (R34 C1-C7) fails. Demotions active in the pass-branch
% main.tex DO NOT bind this fallback: the ridge/M1 evidence that R36 cuts from
% the pass paper becomes THIS paper's protagonist and returns to its main text.
%
% TRIGGER: any DLGI confirmatory bar fails (R34 C1-C7; the feasibility probe
%   already failed bar 4 -- materials map s4a). On trigger, do not leave a
%   weakened dual-product paper in place; switch cleanly to the paper below.
% =============================================================================

% ------------------------------------------------------------------ THESIS ----
% FAIL-BRANCH THESIS -- FROZEN VERBATIM (R36 s7):
%   "Hidden-state uncertainty creates a finite operating boundary in bucket
%    imaging, and matched design-side and inference-side closure tests show that
%    global control is the practical optimum of the declared integrated-count
%    class."
%
% (This supersedes the R35 s1.2 fail-safe thesis, which read: "Hidden dynamics
%  set a finite information-optimal ridge in bucket imaging, and double-sided
%  preregistered closure tests show that global power control is the practical
%  optimum of the declared integrated-count scene-recovery class." R35 s1.2 is
%  retained in FROZEN_REGISTER as the prior wording; use the R36 s7 sentence.)

% ------------------------------------------------------------------- TITLE ----
% Use an R35 protagonist-switch title (R35 s1.3 #7/#8; forbidden-word list binds):
%   7. Hidden-state information sets the operating boundary of ghost imaging
%   8. Dynamic bucket imaging at its information boundary

% ------------------------------------------------------- HARD CAPS (R36 s7) ----
%   * <= 6.2 composed pages including references.
%   * <= 3 main figures.
%   * NO DLGI result figure, NO "second product", NO certification language.

% --------------------------------------------------- SECTION SPINE (R35 s8) ----
% Protagonist = the law/boundary result. Restore the ridge + M1 evidence that the
% pass paper demotes to Supplement S4. Suggested printed order:
%   1. Introduction -- hidden-state limit of bucket imaging (open on the
%      information boundary, NOT on the dual-product inversion).
%   2. Hidden-state information + jitter-capped ridge (Fig. 1: missing-info
%      identity + jitter cap; RESTORE ridge law to main text).
%   3. One global control reaches the ridge (Fig. 2: the M1 imaging/time
%      evidence; RESTORE +1.87 dB / 19.13x -- register B1 sentences VERBATIM,
%      the binding cut does NOT apply in this branch).
%   4. Closing the integrated-count class (Fig. 3: double-sided closure; here it
%      is the CULMINATION, not a bridge).
%   5. Discussion -- boundary, scope, bench transfer; DLGI appears ONLY as a
%      preregistered, theorem-backed outlook whose confirmatory certification
%      failed, with the failed campaign endpoint DISCLOSED.
%   Methods + provenance capsule.

% ------------------------------------------------ FAIL-BRANCH FIGURES (R36 s7)--
%   1. hidden-state missing information + jitter-capped ridge
%        (source: paper2/figs/fig_mechanism.pdf + fig_jitter_validation.pdf).
%   2. the M1 imaging/time evidence
%        (source: paper2/figs/fig_speed_results.pdf; register B1 numbers).
%   3. the double-sided closure of the count-only class
%        (source: gap G4 schematic + five frozen certs, materials map s3a).

% ------------------------------------------------- TRANSITIONS FOR THIS BRANCH --
% The R35 transitions RETIRED from the pass paper are LEGITIMATE here (they were
% retired for opening the demoted M1/ridge movement, which is exactly this
% paper's spine). Reinstate as needed:
%   * "The theorem identifies a finite operating point; the next question is
%      whether one scalar control can realize it in imaging." (theory -> M1)
%   * "A global dial reaches the ridge, but that does not yet show that richer
%      count-only design or inference cannot materially surpass it." (M1 ->
%      closure)
%   * Closure culmination beam (R35 s11): "Within the declared count-only class it
%      cannot..." -- but DROP the "reusing the hidden channel as a second output"
%      tail (there is no second-output movement in this branch).

% --------------------------------------------------- DLGI DISPOSITION (R36 s7) --
% DLGI moves WHOLESALE to the supplement or a later method note, with its failed
% confirmatory endpoint disclosed. NO "second product" / certification language
% in this branch's main text, title, abstract, or figures.

% ------------------------------------------------------- VENUE (R35 s8.3) ------
%   * If DLGI fails ONLY interval coverage while point precision, model validity,
%     scene noninferiority, and reciprocity hold: one Optica attempt remains
%     defensible as a boundary paper with a concise DLGI outlook.
%   * If DLGI fails a substantive information/model/scene bar: skip the
%     high-concept dual-output framing; submit the boundary paper to Physical
%     Review Applied first, then OE if needed.

% =============================================================================
% End of fail-branch spec. Promote to main_law.tex only on a failed C1-C7.
% =============================================================================
