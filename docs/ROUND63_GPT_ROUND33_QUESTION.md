# R33 — Dual-ledger imaging: fence + partition-theorem round (backup mainline activated)

MOLT flagship was killed by the R32 six-bar table (bars 4/6:
end-to-end timing only in a corner regime; the R32.1 fiber closes
geometrically but the noisy equal-time joint reconstruction only TIES
the linear comparator, +0.8 dB — fiber perturbations carry
sub-order-one Δp2). Full evidence @ cf3b3db. The Masked Laplace–
Power-Sum theorem and all verifications are preserved as a
materials-bank conditional result. Per the frozen tree, the backup
mainline activates: DUAL-LEDGER IMAGING.

## The claim

Every GI acquisition through a dynamic medium currently treats the
gain path a_t as nuisance. The dual-ledger claim: the SAME bucket
record is simultaneously (1) a camera for the scene and (2) an
instrument measuring the medium's dynamics (t_c, CV, temporal
spectrum) — zero extra photons, zero extra exposures, no pilots, no
reference arm. The information partition is the HSGI identity itself:
the scene-channel loss term Mᵀ𝔼[Cov(a|Y)]M IS (up to the map) the
medium channel's remaining uncertainty — one record, two ledgers,
exact complementarity. The O4-A moment conditions become a DIAL:
paired schedules protect the scene and blind the medium channel;
anti-paired maximize medium sensitivity; random sits between. Seed
evidence: the ladder probe's state-space smoother recovered the gain
path at correlation 0.998 from plain bucket records (committed).

A feasibility probe is running now: medium-parameter precision
(t_c, CV) pilot-free vs pilot-interleaved vs oracle monitor at equal
budgets; the complementarity trade curve across schedules; the
schedule dial table; zero-cost audit.

## R33 asks

1. **Partition theorem** (you may do the main proof): state and prove
   the exact complementarity/conservation form — the decomposition of
   the record's total Fisher information about (x, medium parameters
   θ = (t_c, CV) or the path a) into scene ledger, medium ledger, and
   the cross/confusion term; conditions under which the O4-A moment
   condition MᵀR⁻¹D_sH = 0 exactly zeroes the cross term (the dial's
   two endpoints). Is there a clean conserved quantity along the
   schedule family (the "two-ledger conservation law")? If the naive
   conservation claim is false, state the correct inequality/identity.
2. **Identifiability of the medium channel**: from one masked bucket
   acquisition with unknown scene, when are (t_c, CV) identifiable
   (gauge issues: global scale confounds; fast-drift limit; slow-drift
   limit where the path is one effective sample)? Give the Fisher
   scaling of t_c-hat and CV-hat with acquisition length N, t_c, CV.
3. **Ruthless fence** (with DOIs): diffuse correlation spectroscopy /
   speckle correlometry (medium dynamics from intensity correlations —
   the closest optical relative; what exactly is new vs DCS with a
   single-pixel detector?); radio-interferometry selfcal gain solutions
   as science products (ionosphere studies from calibration solutions!);
   adaptive-optics turbulence identification from wavefront-sensor
   telemetry; joint blind calibration literature; 'interpolated
   monitoring' CGI (the pilot baseline). What survives: we believe the
   candidate square is "simultaneous certified scene+medium delivery
   from ONE unmodified GI record with an exact information-partition
   theorem and a schedule dial" — check it.
4. **Judgment against the operator's bar** (one exquisite sledgehammer,
   large robust universal gains, 创新简洁普适美观): is the dual-ledger
   framing a genuine eye-lighter or another competent-but-boring
   integration? Be blunt. If the probe numbers (due in ~2h) show
   pilot-free within 1.5x of pilot precision at zero scene cost, what
   is the sharpest honest headline?
5. Names (2-3 candidates).

Deliver as a GitHub issue on ccyyyYyzz/GI_a2 titled R33. Reference
commit stated in chat.
