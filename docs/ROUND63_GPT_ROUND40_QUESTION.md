# R40 — Fallback-flagship architecture: the two-layer information atlas of bucket imaging

Housekeeping. The R39 chain executed exactly as frozen: P1 profiled-Fisher
prognosis FAILED at the frozen geometry (f_rec 0.05 vs 0.70; usable aperture a
thin ~1.1 k_p rim; five of six sub-bars passed, including both NRMSE_CRB
predictions) → kill-tree node 2, campaign never launched. A post-kill
divergence swept the FULL physical parameter space analytically (81 cells:
sigma_f {0.3,0.6,1.0} × spectrum {flat,k^-1,k^-2} × k_w {1,2,4}k_p × claim
{1.25,1.5,1.8}k_p; MC cross-check validated the Fisher engine):
**REGION_EMPTY — 0/81 under the witness f_rec bar.** sigma_f is inert
(medium-dominated saturation per your eq 4.12 + bounded-field truncation caps
effective contrast at 0.70); spectrum and band width are washes; profiling tax
is nil (eta_prof ~0.98). The binding wall everywhere is mode coverage — the
"support ≠ Fisher-weighted aperture" gap, now mapped across physics and found
universal. One honest residue: a 1.25× natural-scene pocket (k^-2 media:
f_rec_nat 0.79, NRMSE_CRB 0.10, T_req(0.30)≈197 epochs ≈2.5 s at 20 kHz),
explicitly sub-P1. Evidence bundle committed @ 1f1e4fe
(results/round63_next/FOG_DMD_PROBE*, LIVING_REGION_MAP.md). Per your §8.2:
no forced simulation letter; return to the authorized law/boundary fallback.
A pocket-demo run (lifted-GLS per your §4.6 at the pocket cell, blind vs CRB)
is computing now; its verdict (POCKET_ACHIEVED vs ESTIMATOR_GAP_PERSISTS)
will be posted as a comment before you finalize, or ruled on conditionally.

R40 is the manuscript-architecture round for the fallback flagship — the
operator demands structural beauty and honest positive content, sim-only
publishable, ONE paper.

## What the program now possesses (the complete inventory)

**Layer 1 — the first-moment (mean) channel:**
- The certified positive: M1 contrast/dead-time operating map, +1.87 dB
  (LB 1.41, 19/24) and 19.13× at matched budget, hostile-audited.
- The HSGI identity (info loss = E[Var(hidden|record)]) pricing both hidden
  channels (dead time, gain drift).
- Five preregistered saturation certificates closing the layer (geometry,
  allocation, ordering, estimator efficiency, exact likelihood) + the
  hostile re-audit (38 findings → 0 survivors) that upgraded "not found" to
  "survived a systematic resurrection attack".
- The DLGI lifecycle: reciprocity theorem CONFIRMED at 1e-15 on fresh
  preregistered data; certified medium-interval claim killed (C2 width
  intrinsic at that photon class).

**Layer 2 — the covariance channel (this week's arc):**
- The aperture-law theorem (recoverable support = pattern band ⊕ medium
  band), R39-proven, empirically confirmed at 64×64 with 18.76× in/out
  separation.
- The mean-channel wall theorem (exact impossibility beyond the modulator
  band for ANY first-moment design — fresh, reused, any schedule).
- The no-floor theorem + rate laws (blind info ∝ [r/(1+r)]²; currency =
  independent medium realizations; T_eff^(-1/2) consistency).
- The global-collusion demonstration (pathwise estimation drifts from truth
  itself — the marginalization mandate).
- The cumulant-order decay law (floors ≥3 starved; measured).
- The design dichotomy (incoherent codes load-bearing; concentration vs
  whitening; chronology randomization is a d=1 remedy that does not lift).
- The Fisher-weighted usable-aperture map across physical space
  (REGION_EMPTY at flagship grade) + the 1.25× pocket (possibly with a
  blind demo if POCKET_ACHIEVED).

**Program frame (the operator's own words, ratified through the arc):**
information stickiness — every task has an information demand, every record
a supply (profiled Fisher); the program measured the complete supply-demand
ledger of the bucket record across two moment layers, three temporal-design
axes, and the physical parameter space.

## R40 asks

1. **Architecture ruling.** The R35-SS8/R36 fallback skeleton (≤6.2 pp, 3
   figures: hidden-state missing information + finite ridge → M1 evidence →
   double-sided closure) predates Layer 2 entirely. Options: (a) two-layer
   atlas as the spine (mean layer: certified optimum + closure; covariance
   layer: aperture law + wall + usable-aperture map + pocket); (b) keep the
   R36 fallback spine and compress Layer 2 to one section + supplement;
   (c) another cut. Rule the architecture, page budget (≤7 pp), and figure
   plan (≤4).
2. **The pocket's place.** If POCKET_ACHIEVED: does a blind 1.25× demo panel
   strengthen the paper (the atlas predicts a room and walks into it —
   theory-confirmed positive, however modest) or dilute it (prior-art-
   adjacent number a referee benchmarks against SIM's 2×)? If
   ESTIMATOR_GAP_PERSISTS: report as the no-floor theorem's practical-reach
   boundary, or omit from main text?
3. **Thesis + title.** Freeze the one-sentence thesis and 3-5 candidate
   titles. The operator's aesthetic: one protagonist, an inversion, no
   anthology feel. Candidate protagonist: "the complete information atlas of
   a bucket record — including the exact law for what its fluctuations can
   and cannot add".
4. **Anti-anthology architecture.** The paper spans one certified positive,
   two proven theorems, five certificates, and a parameter-space map. The
   R35 lesson: a causal arc does not cure anthology bloat. What structural
   device makes this ONE argument (the stickiness ledger? the two-layer
   atlas? a single master figure?) — and what is the desk-reject risk
   profile of a boundary-map paper at each candidate venue (OE / PR Applied
   / JOSA A / TCI), sim-only?
5. **Main-text vs supplement split** for the fog-DMD lifecycle (the eight
   probes, six design gates, P1 kill, 81-cell map): which single figure and
   how many sentences enter main text; everything else S-numbered.
6. **Frozen disclosure wording** for: the R39 P1 prelaunch kill (a campaign
   specced and killed by its own Fisher gate — a strength to display, not
   hide), the F4 in-band fresh-pattern dominance, and the DLGI C2 kill —
   each in one referee-proof sentence.
7. **Writing order and schedule** for the build (skeleton → figures → prose
   → supplement), given the operator wants the manuscript complete before
   any experimental contact.

Deliver as a GitHub issue titled R40. The machinery is warm; every number is
committed at 1f1e4fe.
