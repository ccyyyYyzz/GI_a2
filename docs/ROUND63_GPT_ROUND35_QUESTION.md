# R35 — Flagship manuscript architecture (conception, logic, appeal, compactness)

The operator has ordered full speed on the endgame and explicitly
delegated the hard editorial thinking to you: how to CONCEIVE the one
flagship manuscript so it is logical, attractive, and tight. This round
runs in parallel with the DLGI confirmatory campaign (R34 GO; scaffold
in build). Architecture now, prose after the campaign verdict.

## Frozen inputs (all committed)

- **The ruled arc (R34 §5)**: "first quantify hidden-channel
  information, then show the saturation boundary, finally reuse one
  record to deliver a second scientific product." DLGI conditionally
  leads the METHOD act; no standalone split.
- **Act I material**: E1/O1 hidden-state identities (Louis/Orchard-
  Woodbury specializations, honest lineage), jitter-capped ridge law
  J(ρ;c), universal exponents (1/3, −2/3) blind-verified, R16 estimator-
  artifact closure; correction-ceiling impossibility (count-only
  software cannot recover jitter loss).
- **Act II material**: M1 certified campaign — ridge tracking +1.87 dB
  median (LB 1.41, 19/24) and 19.13× time-to-quality speedup (LB 18.33,
  21/24), R19-corrected, fully audited; operating maps.
- **Act III material**: FIVE preregistered negative certificates on
  bucket-count channels — geometry-library reachability +0.68 dB < 1.0
  bar (k* always trivial), allocator harm −8.3 dB, schedule optimization
  +0.04 dB, estimator efficiency ~99% (≤0.2 dB headroom), exact
  conditional likelihood +0.41 dB < 1.0. Double-sided bracketing: the
  global operating law is the practical optimum of the class, proven
  from the software side and measured from the design side. Plus the
  MOLT materials-bank theorem (Masked Laplace–Power-Sum; killed at
  image level by three independent computations) — candidate for
  supplement or a paragraph.
- **Act IV material (conditional)**: DLGI — Canonical-Confusion Ledger
  Reciprocity theorem (verified 1e-15), pilot-free medium estimation
  within 1.5× of (usually better than) pilot at zero acquisition cost,
  scene noninferior/superior, honest edge map; campaign C1-C7 pending;
  authorized headline "one bucket stream, two model-certified products /
  ghost imaging becomes its own medium monitor".
- **Venue plan**: try Optica first (concise, high-concept form),
  PR Applied fallback, OE floor. Pure simulation — no bench experiment;
  the DLGI medium-monitor claim is deliberately easy to validate on a
  rotating-diffuser bench later (strategic hook for experimental
  collaboration).
- **Operator's aesthetic**: 创新 (novelty), 简洁 (economy), 普适
  (generality), 美观 (elegance); compression over addition; lead with
  the surprising mechanism, not infrastructure; no defensive sprawl.

## R35 asks

1. **The thesis**: one sentence the whole paper argues. Then 5-8 title
   candidates (Optica register: concept-first, no acronym soup).
2. **Architecture**: act-by-act section plan with a hard page budget
   for the Optica-first form (main text lean; supplement absorbs depth).
   For each section: its single claim, its one indispensable
   figure/table, and what it must NOT contain. State exactly which
   material goes to the supplement wholesale (e.g., the five
   certificates' full campaign tables, MOLT, operating-map details).
3. **The negative-results problem**: Act III is five preregistered
   negatives — scientifically load-bearing (it motivates Act IV and
   kills the "why not just adapt/learn?" referee question) but a
   narrative slog if listed. Design the presentation that makes the
   saturation boundary FEEL like a discovery (one figure? one table?
   one paragraph each?) rather than a graveyard tour.
4. **The hook**: opening-paragraph strategy — what problem statement
   makes an optics reader need this paper; where the "detector/medium
   nuisance becomes a second instrument" inversion lands (first
   paragraph? after the setup?).
5. **The hero figure**: describe THE figure (concept sketch, panels,
   what a referee should understand in 10 seconds). Plus the full
   figure inventory (target: 4-5 main figures max).
6. **Referee pre-emption**: the five most likely attacks (no
   experiment; "just Kalman smoothing"; DCS priority; simulation-only
   generality; missing-information machinery is classical) and how the
   ARCHITECTURE (not defensive prose) neutralizes each.
7. **The fail-branch variant**: if the DLGI campaign fails C1-C7, the
   same skeleton must degrade gracefully to a law/boundary paper —
   mark which sections change and whether the thesis sentence survives.
8. Anything you would cut that we have not thought to cut.

Deliver as a GitHub issue on ccyyyYyzz/GI_a2 titled R35. Depth on
architecture; this becomes the frozen outline the writing executes.
